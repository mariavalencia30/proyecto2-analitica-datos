# Proyecto PENGWIN — Corte 2, semana 8
# Python 3.11 o 3.12. Instalar con:
#   pip install -r requirements.txt

SimpleITK
# Familia compatible con torch 2.2 en macOS x86_64. Torch 2.2 se distribuye
# contra NumPy 1.x; fijar también sus extensiones evita incompatibilidades ABI.
numpy<2
pandas<3
matplotlib<3.10
tqdm
scipy<1.15
scikit-image<0.25
plotly
torch
